export class BiAnomalyThresholdCli {
  public static async run(action: string, args: string[]): Promise<void> {
    console.log("[CLI-ENTITY] Performing " + action + " for BiAnomalyThreshold with args:", args);
  }
}
