export class BiAggregatedDailyMetricCli {
  public static async run(action: string, args: string[]): Promise<void> {
    console.log("[CLI-ENTITY] Performing " + action + " for BiAggregatedDailyMetric with args:", args);
  }
}
