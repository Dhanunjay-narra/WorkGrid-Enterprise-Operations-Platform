export class SupportTicketsSnapshotConsumer {
  public async handleCreated(event: { id: string; tenantId: string; entityId: string; occurredAt: string }): Promise<void> {
    console.log("[EVENT-BUS] Consumed SupportTicketsSnapshot created event for entity " + event.entityId + " in support_tickets");
  }

  public async handleUpdated(event: { id: string; tenantId: string; entityId: string; delta: Record<string, any> }): Promise<void> {
    console.log("[EVENT-BUS] Consumed SupportTicketsSnapshot updated event for entity " + event.entityId + " in support_tickets");
  }

  public async handleDeleted(event: { id: string; tenantId: string; entityId: string }): Promise<void> {
    console.log("[EVENT-BUS] Consumed SupportTicketsSnapshot deleted event for entity " + event.entityId + " in support_tickets");
  }
}
