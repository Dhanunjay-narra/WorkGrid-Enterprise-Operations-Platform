export class BiExportsTransactionConsumer {
  public async handleCreated(event: { id: string; tenantId: string; entityId: string; occurredAt: string }): Promise<void> {
    console.log("[EVENT-BUS] Consumed BiExportsTransaction created event for entity " + event.entityId + " in bi_exports");
  }

  public async handleUpdated(event: { id: string; tenantId: string; entityId: string; delta: Record<string, any> }): Promise<void> {
    console.log("[EVENT-BUS] Consumed BiExportsTransaction updated event for entity " + event.entityId + " in bi_exports");
  }

  public async handleDeleted(event: { id: string; tenantId: string; entityId: string }): Promise<void> {
    console.log("[EVENT-BUS] Consumed BiExportsTransaction deleted event for entity " + event.entityId + " in bi_exports");
  }
}
