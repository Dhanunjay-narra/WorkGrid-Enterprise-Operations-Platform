export class CommThreadsEventConsumer {
  public async handleCreated(event: { id: string; tenantId: string; entityId: string; occurredAt: string }): Promise<void> {
    console.log("[EVENT-BUS] Consumed CommThreadsEvent created event for entity " + event.entityId + " in comm_threads");
  }

  public async handleUpdated(event: { id: string; tenantId: string; entityId: string; delta: Record<string, any> }): Promise<void> {
    console.log("[EVENT-BUS] Consumed CommThreadsEvent updated event for entity " + event.entityId + " in comm_threads");
  }

  public async handleDeleted(event: { id: string; tenantId: string; entityId: string }): Promise<void> {
    console.log("[EVENT-BUS] Consumed CommThreadsEvent deleted event for entity " + event.entityId + " in comm_threads");
  }
}
